# Copy this file to .env and fill in your key.
# The app reads GROQ_API_KEY from the environment (e.g. via `export` or
# a tool like python-dotenv / `streamlit run` picking up a .env loader).
GROQ_API_KEY=your_groq_api_key_here
